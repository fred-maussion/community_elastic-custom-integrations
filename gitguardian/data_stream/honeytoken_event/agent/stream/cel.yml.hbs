config_version: 2
{{#if condition}}
condition: '{{condition}}'
{{/if}}
interval: {{interval}}
resource.tracer:
  enabled: {{enable_request_tracer}}
  filename: "../../logs/cel/http-request-trace-*.ndjson"
  maxbackups: 5
{{#if proxy_url}}
resource.proxy_url: {{proxy_url}}
{{/if}}
{{#if proxy_headers}}
resource.proxy_headers: {{proxy_headers}}
{{/if}}
{{#if ssl}}
resource.ssl: {{ssl}}
{{/if}}
{{#if http_client_timeout}}
resource.timeout: {{http_client_timeout}}
{{/if}}

resource.url: {{url}}

state:
  limit: {{batch_size}}
  url: {{url}}
  api_token: {{api_key}}
  honeytoken_status: "{{honeytoken_status}}"

redact:
  fields:
    - api_token

program: |-
  state.with(
    get_request(
      (has(state.?cursor.next_url) && state.cursor.next_url != "") ?
        state.cursor.next_url
      :
        state.url.trim_right("/") + "/honeytokens_events?" + (
          {
            "per_page": [string(int(state.limit))],
            "ordering": ["triggered_at"],
          } + (
            (has(state.honeytoken_status) && state.honeytoken_status != "") ?
              {"status": [state.honeytoken_status]}
            : {}
          )
        ).format_query()
    ).with(
      {
        "Header": {
          "Accept": ["application/json"],
          "Authorization": ["Token " + state.api_token],
        },
      }
    ).do_request().as(resp,
      resp.StatusCode == 429 ?
        {
          "events": [],
          "cursor": state.?cursor.orValue({}),
          "want_more": false,
        }
      : (resp.StatusCode != 200) ?
        {
          "events": {
            "error": {
              "code": string(resp.StatusCode),
              "id": string(resp.Status),
              "message": "GET:" + (
                (size(resp.Body) != 0) ?
                  string(resp.Body)
                :
                  string(resp.Status) + " (" + string(resp.StatusCode) + ")"
              ),
            },
          },
          "want_more": false,
        }
      :
        bytes(resp.Body).decode_json().as(body, size(body) == 0 ?
          {
            "events": [],
            "cursor": {
              "next_url": state.?cursor.next_url.orValue(""),
            },
            "want_more": false,
          }
        :
          resp.Header[?"Link"].orValue([]).map(h,
            h.split(",").map(link,
              link.split(";").as(parts,
                (size(parts) > 1 && parts[1].matches('rel="?next"?')) ?
                  parts[0].trim(" <>")
                : ""
              )
            )
          ).flatten().filter(u, u != "").as(nexts,
            {
              "events": body.map(entry, {"message": entry.encode_json()}),
              "cursor": {
                "next_url": (size(nexts) > 0) ? nexts[0] : "",
              },
              "want_more": size(nexts) > 0,
            }
          )
        )
    )
  )
tags:
{{#if preserve_original_event}}
  - preserve_original_event
{{/if}}
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{#contains "forwarded" tags}}
publisher_pipeline.disable_host: true
{{/contains}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
